# Eat-n-Split 🍽️

This is the **Eat-n-Split App** built with **React** while following Jonas Schmedtmann’s React course.
It’s a practice project to learn state management, props, conditional rendering, and lifting state up.

---

## 🧩 Installation

- Install dependencies:

  ```bash
  npm install
  ```

- Start the dev server:

  ```bash
  npm start
  ```

- Open [http://localhost:3000](http://localhost:3000).

---
